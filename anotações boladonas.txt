janela 80/25
abrir o programa como administrador
desativar opção de back up:
  exc -> menu -> options -> back ups -> enter para off -> save current settings
XX.prg
hbmk2 XX.prg (criar arquivo executavel)
dir(checa se criou)
XX.exe (executa o arquivo)

q XX.prg para criar um arquivo ou para entrar neste arquivo

set scoreBoard off
	elimina tracinho que apaga o quadro

replicate(caracter para replicar,quantidade)

emite um som em uma frequencia de livre escolha por um numero determinado de tempo
Tone(Frequencia,tempo)
Tone(500,1)

temporizador
inKey(xtempo)
	retorna o codigo da tecla pressionada

quando se soma ou subtrai uma data, ele sempre ira faze-lo em dias